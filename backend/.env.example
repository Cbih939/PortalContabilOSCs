# --- .env.example ---
# Ficheiro de exemplo para as variáveis de ambiente do Backend.
# Copie este ficheiro para '.env' e preencha com os seus valores reais.
# NÃO coloque informações sensíveis neste ficheiro (.env.example).

# --- Configuração do Servidor ---
NODE_ENV=development
PORT=5000

# --- Configuração do Banco de Dados MySQL ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=sua_senha_mysql_aqui
DB_NAME=contabil_osc_db

# --- Configuração de Segurança (JWT) ---
# Use um gerador de senhas fortes para o JWT_SECRET no ficheiro .env real
JWT_SECRET=seu_jwt_secret_forte_aqui
JWT_EXPIRES_IN=7d