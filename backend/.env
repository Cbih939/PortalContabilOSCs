# --- .env ---
# Configurações de Ambiente para o Backend do Portal Contábil OSCs
#
# !! IMPORTANTE !!
# 1. Substitua os valores de exemplo abaixo pelos seus dados reais.
# 2. NUNCA envie este ficheiro para o Git. Adicione '.env' ao seu '.gitignore'.
# 3. Crie um ficheiro '.env.example' (sem as senhas) para incluir no Git.

# --- Configuração do Servidor ---
NODE_ENV=development  # Mude para 'production' na VPS
PORT=5000             # Porta onde a API backend irá rodar

# --- Configuração do Banco de Dados MySQL ---
DB_HOST=localhost         # Endereço do seu servidor MySQL (pode ser '127.0.0.1' ou um IP)
DB_PORT=3306              # Porta padrão do MySQL
DB_USER=root              # Nome de utilizador do MySQL (use um utilizador específico em produção)
DB_PASSWORD=  # !! Substitua pela senha do seu utilizador MySQL !!
DB_NAME=portal_contabil   # Nome do banco de dados que as migrações irão criar/usar

# --- Configuração de Segurança (JWT) ---
# Use um gerador de senhas fortes online para criar um segredo longo e aleatório
JWT_SECRET=SEU_SEGREDO_JWT_LONGO_E_SEGURO_AQUI # !! Substitua por um segredo forte !!
JWT_EXPIRES_IN=7d         # Duração do token de login (ex: '1h', '30m', '7d')